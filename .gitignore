node_modules/
dist/
.DS_Store
.vercel
*.log

# Build artifacts — regenerated by scripts/build-images.mjs on every build
# (locally via the pre-dev / pre-build hooks, and on Vercel via `npm run build`).
# Source images stay tracked; only these optimized outputs + manifests are ignored.
**/_opt/
public/work.json
public/stickers.json
public/tags.json
public/about.json
public/feed.json
public/posters.json

# Local-only: Instagram promo graphics moved out of the stickers gallery
stickers-promo/
